{
  "buildCommand": "npm install -g pnpm && cd electron-app && pnpm install && pnpm run build:web",
  "outputDirectory": "electron-app/dist-web",
  "framework": "vite",
  "installCommand": "echo 'Skipping default install'"
}
